import React from 'react'
import ScrollWrapper from '../../utils/ScrollWrapper'

const SkillCard = () => {
  return (
    <ScrollWrapper>
        
    </ScrollWrapper>
  )
}

export default SkillCard
